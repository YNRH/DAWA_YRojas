// En randomBackground.js

const elements = document.querySelectorAll(".background-image");

elements.forEach((element) => {
  element.style.backgroundColor = "rgba(0, 0, 0, 0.5)"; // Capa negra con transparencia
});
